specVersion: "v1beta"
serviceId: "m1autoposter"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "m1autoposter-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
